# yvolo

Aplicación enfocada únicamente al español por ahora. La versión en inglés ("yfly") se implementará en el futuro cuando yvolo esté estable.

## Cómo ejecutar

    python ui_main.py

## Cómo ejecutar los tests

    python -m unittest discover -s tests -p "test_*.py" -v
